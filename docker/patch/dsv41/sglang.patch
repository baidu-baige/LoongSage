diff --git a/python/sglang/srt/debug_utils/dumper.py b/python/sglang/srt/debug_utils/dumper.py
index 111217f108..260295d958 100644
--- a/python/sglang/srt/debug_utils/dumper.py
+++ b/python/sglang/srt/debug_utils/dumper.py
@@ -1805,12 +1805,18 @@ class _SGLangPlugin(_FrameworkPlugin):
 
 
 class _MegatronPlugin(_FrameworkPlugin):
-    _available = True
-    try:
-        from megatron.core import parallel_state as _mpu
-        from megatron.core.packed_seq_params import PackedSeqParams
-    except ImportError:
-        _available = False
+    # NOTE(coda): eager `import megatron.core` disabled on purpose.
+    # In colocate mode the sglang engine process loads weights via Megatron-FSDP;
+    # importing megatron flips torch_memory_saver's global hook_mode to "torch",
+    # which breaks sglang's pauseable CUDA graph (needs hook_mode="preload") and
+    # crashes startup with "Only hook_mode=preload supports pauseable CUDA Graph".
+    # The dumper is unused in the engine process, so force _available=False.
+    _available = False
+    #try:
+    #    from megatron.core import parallel_state as _mpu
+    #    from megatron.core.packed_seq_params import PackedSeqParams
+    #except ImportError:
+    #    _available = False
 
     @property
     def name(self) -> str:
diff --git a/python/sglang/srt/entrypoints/http_server.py b/python/sglang/srt/entrypoints/http_server.py
index f5d3f1f045..671c7144b0 100644
--- a/python/sglang/srt/entrypoints/http_server.py
+++ b/python/sglang/srt/entrypoints/http_server.py
@@ -144,6 +144,7 @@ from sglang.srt.managers.io_struct import (
     UpdateWeightFromDiskReqInput,
     UpdateWeightsFromDistributedReqInput,
     UpdateWeightsFromIPCReqInput,
+    UpdateWeightsFromChannelReqInput,
     UpdateWeightsFromTensorReqInput,
     UpdateWeightVersionReqInput,
     VertexGenerateReqInput,
@@ -1382,6 +1383,19 @@ async def destroy_weights_update_group(
     )
 
 
+@app.post("/update_weights_from_channel")
+@auth_level(AuthLevel.ADMIN_OPTIONAL)
+async def update_weights_from_channel(
+    obj: Annotated[UpdateWeightsFromChannelReqInput, Body()], request: Request
+):
+    """Update weights by receiving from the TransferMesh channel."""
+    success, message = await _global_state.tokenizer_manager.update_weights_from_channel(
+        obj, request
+    )
+    content = {"success": success, "message": message}
+    return ORJSONResponse(content, status_code=200 if success else HTTPStatus.BAD_REQUEST)
+
+
 @app.post("/update_weights_from_tensor")
 @auth_level(AuthLevel.ADMIN_OPTIONAL)
 async def update_weights_from_tensor(
diff --git a/python/sglang/srt/layers/attention/deepseek_v4_backend.py b/python/sglang/srt/layers/attention/deepseek_v4_backend.py
index 358ac92c28..59d3aaaf26 100644
--- a/python/sglang/srt/layers/attention/deepseek_v4_backend.py
+++ b/python/sglang/srt/layers/attention/deepseek_v4_backend.py
@@ -2649,6 +2649,27 @@ class DeepseekV4AttnBackend(
             )
             return
         meta = self.forward_metadata
+        # The per-DP batch width is padded up to attn_tp_size before the forward
+        # (prepare_mlp_sync_batch), so an eager `positions` can carry trailing
+        # pad rows while `extend_seq_lens` only covers the live tokens. The
+        # compressor/indexer rebuild per-token request ids from those lengths
+        # (`repeat_interleave(..., output_size=positions.shape[0])`), which needs
+        # the two to agree, so run them over the live prefix and let the
+        # existing pad masks drop the remaining rows.
+        in_prefill_graph = self._low_ratio_in_prefill_graph()
+        if (
+            forward_batch.forward_mode.is_extend()
+            and not in_prefill_graph
+            and getattr(meta, "late_layer_tail", None) is None
+            and forward_batch.extend_seq_lens_cpu is not None
+            and x.shape[0] == positions.shape[0]
+        ):
+            live_tokens = sum(int(n) for n in forward_batch.extend_seq_lens_cpu)
+            if 0 <= live_tokens < positions.shape[0]:
+                x = x[:live_tokens]
+                if q_lora is not None:
+                    q_lora = q_lora[:live_tokens]
+                positions = positions[:live_tokens]
         hoisted_req = getattr(meta, "low_ratio_req_indices", None)
         hoisted_pos = getattr(meta, "low_ratio_pos_i64", None)
         if (
@@ -2662,10 +2683,7 @@ class DeepseekV4AttnBackend(
         else:
             req = token_req_indices(forward_batch, num_tokens=positions.shape[0])
             pos = positions.to(torch.int64)
-        if (
-            forward_batch.forward_mode.is_extend()
-            and self._low_ratio_in_prefill_graph()
-        ):
+        if forward_batch.forward_mode.is_extend() and in_prefill_graph:
             bufs = self._source_projection_buffers(x.shape[0], layer.compress_ratio)
             _bcg_low_ratio_source_projections(layer, x, q_lora, pos, bufs)
             if run_compressor and layer.compressor is not None:
diff --git a/python/sglang/srt/layers/engram.py b/python/sglang/srt/layers/engram.py
index 874622b6f0..2861b1b1f9 100644
--- a/python/sglang/srt/layers/engram.py
+++ b/python/sglang/srt/layers/engram.py
@@ -281,6 +281,17 @@ class EngramHasher(nn.Module):
                 device=input_ids.device,
             )
         mode = forward_batch.forward_mode
+        if mode.is_idle():
+            # DP-attention MLP-sync dummy batch (padded idle). Its output is
+            # discarded and it must NOT mutate engram history. Under CUDA graph
+            # this is folded into the decode graph (ForwardMode.is_cuda_graph()
+            # includes IDLE); only the eager path (disable_cuda_graph) reaches
+            # here with a padded idle batch, so return throwaway zero hashes.
+            return torch.zeros(
+                (num_tokens, self.primes.shape[0], self.offsets.shape[1]),
+                dtype=torch.int64,
+                device=input_ids.device,
+            )
         req_slots = forward_batch.req_pool_indices
         bs = req_slots.shape[0]
         device = input_ids.device
@@ -970,3 +981,63 @@ class Engram(nn.Module):
         return engram_gate(
             x, kv, self.q_weight, self.k_weight, self.eps, self.clamp_value
         )
+
+
+def load_engram_tables_from_disk(model: nn.Module, model_path: str) -> None:
+    """Load the frozen engram fp8 tables from ``model_path`` into the model's
+    ``EngramEmbedding`` modules.
+
+    Required for ``load_format=dummy`` (the RL rollout path): the dummy loader
+    skips ``model.load_weights()`` and fills every parameter — including the engram
+    ``weight``/``scale`` — with random values. The engram tables are constants that
+    the RL weight update does NOT carry, so without this they would stay garbage and
+    train/rollout parity blows up. This reuses each ``EngramEmbedding``'s own
+    ``weight_loader`` (row-sharded per TP rank) and ``finish_load``; it is a no-op for
+    models that have no engram layers.
+    """
+    import json
+
+    from safetensors import safe_open
+
+    inner = getattr(model, "model", None)
+    layers = getattr(inner, "layers", None)
+    if layers is None:
+        return
+    engram_layers = [
+        (i, layer.engram)
+        for i, layer in enumerate(layers)
+        if getattr(layer, "engram", None) is not None
+    ]
+    if not engram_layers:
+        return
+
+    index_path = os.path.join(model_path, "model.safetensors.index.json")
+    if not os.path.exists(index_path):
+        logger.warning(
+            "engram dummy-load: no safetensors index at %s; engram tables left uninitialized",
+            index_path,
+        )
+        return
+    with open(index_path) as f:
+        weight_map = json.load(f)["weight_map"]
+
+    logger.info(
+        "load_format=dummy: loading engram tables from %s for %d layer(s)",
+        model_path,
+        len(engram_layers),
+    )
+    for i, engram in engram_layers:
+        emb = engram.embed
+        for suffix, param in (("weight", emb.weight), ("scale", emb.scale)):
+            name = f"layers.{i}.engram.embed.{suffix}"
+            shard = weight_map.get(name)
+            if shard is None:
+                raise KeyError(f"engram dummy-load: {name} missing from {index_path}")
+            with safe_open(os.path.join(model_path, shard), framework="pt", device="cpu") as f:
+                # Pass the LAZY safetensors slice (not get_tensor) to the module's own
+                # weight_loader: _load_rows only indexes loaded_weight[row_start:row_end],
+                # so a PySafeSlice reads just this rank's rows from disk and we never
+                # materialize the full ~98GB fp8 table in host RAM per rank (that caused
+                # colocate host OOM). _load_rows also handles the shared/private layout.
+                param.weight_loader(param, f.get_slice(name))
+        emb.finish_load(label=f"dummy engram layer {i}")
diff --git a/python/sglang/srt/layers/layernorm.py b/python/sglang/srt/layers/layernorm.py
index 626381b65d..851e9eed64 100644
--- a/python/sglang/srt/layers/layernorm.py
+++ b/python/sglang/srt/layers/layernorm.py
@@ -48,6 +48,8 @@ from sglang.srt.utils import (
 
 _is_cuda = is_cuda()
 _is_flashinfer_available = is_flashinfer_available()
+import os as _os_no_pdl
+_RMSNORM_NO_PDL = _os_no_pdl.environ.get("SGLANG_RMSNORM_NO_PDL", "0") == "1"
 _is_hip = is_hip()
 _is_musa = is_musa()
 _is_npu = is_npu()
@@ -598,7 +600,7 @@ class RMSNorm(BaseFusedOp):
                 fused_add_rmsnorm(x, residual, self.weight.data, self.variance_epsilon)
                 result = x, residual
         else:
-            result = rmsnorm(x, self.weight.data, self.variance_epsilon)
+            result = rmsnorm(x, self.weight.data, self.variance_epsilon, enable_pdl=(False if _RMSNORM_NO_PDL else None))
 
         if needs_reshape:
             if residual is not None:
@@ -887,7 +889,7 @@ class RMSNorm(BaseFusedOp):
                 residual = residual + post_residual_addition
             fused_add_rmsnorm(x, residual, self.weight.data, self.variance_epsilon)
             return x, residual
-        out = rmsnorm(x, self.weight.data, self.variance_epsilon)
+        out = rmsnorm(x, self.weight.data, self.variance_epsilon, enable_pdl=(False if _RMSNORM_NO_PDL else None))
         return out
 
     def forward_with_allreduce_fusion(
diff --git a/python/sglang/srt/managers/io_struct.py b/python/sglang/srt/managers/io_struct.py
index 764205f08a..e5051b76d5 100644
--- a/python/sglang/srt/managers/io_struct.py
+++ b/python/sglang/srt/managers/io_struct.py
@@ -1956,6 +1956,25 @@ class DestroyWeightsUpdateGroupReqOutput(BaseReq, kw_only=True):
     message: str
 
 
+class UpdateWeightsFromChannelReqInput(BaseReq, kw_only=True):
+    master_addr: str
+    world_size: int
+    train_world_size: int
+    weight_version: str
+    engine_id: int = 0
+    recreate: bool = False
+    buffer_size_bytes: int = 256 * 1024 * 1024
+    timeout: float = 300.0
+    gloo_port: int = 29500
+    flush_cache: bool = True
+    torch_empty_cache: bool = False
+
+
+class UpdateWeightsFromChannelReqOutput(BaseReq, kw_only=True):
+    success: bool
+    message: str
+
+
 class UpdateWeightVersionReqInput(BaseReq, kw_only=True):
     # The new weight version
     new_version: str
diff --git a/python/sglang/srt/managers/scheduler.py b/python/sglang/srt/managers/scheduler.py
index 3f9e0ef027..ec91fa15d9 100644
--- a/python/sglang/srt/managers/scheduler.py
+++ b/python/sglang/srt/managers/scheduler.py
@@ -176,6 +176,7 @@ from sglang.srt.managers.io_struct import (
     UnloadLoRAAdapterReqInput,
     UnloadLoRAAdapterReqOutput,
     UpdateWeightFromDiskReqInput,
+    UpdateWeightsFromChannelReqInput,
     UpdateWeightsFromDistributedReqInput,
     UpdateWeightsFromIPCReqInput,
     UpdateWeightsFromTensorReqInput,
@@ -1747,6 +1748,10 @@ class Scheduler(
                     UpdateWeightsFromIPCReqInput,
                     self.weight_updater.update_weights_from_ipc,
                 ),
+                (
+                    UpdateWeightsFromChannelReqInput,
+                    self.weight_updater.update_weights_from_channel,
+                ),
                 (
                     UpdateWeightVersionReqInput,
                     self.handle_update_weight_version,
diff --git a/python/sglang/srt/managers/scheduler_components/weight_updater.py b/python/sglang/srt/managers/scheduler_components/weight_updater.py
index 925ee721b7..e9051fbabc 100644
--- a/python/sglang/srt/managers/scheduler_components/weight_updater.py
+++ b/python/sglang/srt/managers/scheduler_components/weight_updater.py
@@ -38,6 +38,8 @@ from sglang.srt.managers.io_struct import (
     UpdateWeightsFromDistributedReqOutput,
     UpdateWeightsFromIPCReqInput,
     UpdateWeightsFromIPCReqOutput,
+    UpdateWeightsFromChannelReqInput,
+    UpdateWeightsFromChannelReqOutput,
     UpdateWeightsFromTensorReqInput,
     UpdateWeightsFromTensorReqOutput,
 )
@@ -192,6 +194,20 @@ class SchedulerWeightUpdaterManager:
         parameter = self.tp_worker.get_weights_by_name(recv_req)
         return GetWeightsByNameReqOutput(parameter=parameter)
 
+    def update_weights_from_channel(
+        self, recv_req: UpdateWeightsFromChannelReqInput
+    ):
+        """Update model weights by receiving from the TransferMesh channel."""
+        with self._observe_weight_load("channel"):
+            success, message = self.tp_worker.update_weights_from_channel(recv_req)
+            if success:
+                self.flush_cache_after_weight_update(recv_req)
+                self.record_weight_version_after_update(recv_req.weight_version)
+            else:
+                logger.error(message)
+            torch.distributed.barrier(group=self.tp_cpu_group)
+            return UpdateWeightsFromChannelReqOutput(success=success, message=message)
+
     def _assert_weight_cache_inactive(self, op: str) -> None:
         """Reject freeing/restoring model weights while the CUDA IPC weight
         cache is active: the weights are shared with the daemon via CUDA IPC, so
diff --git a/python/sglang/srt/managers/tokenizer_control_mixin.py b/python/sglang/srt/managers/tokenizer_control_mixin.py
index 857444388d..9466814c04 100644
--- a/python/sglang/srt/managers/tokenizer_control_mixin.py
+++ b/python/sglang/srt/managers/tokenizer_control_mixin.py
@@ -70,6 +70,8 @@ from sglang.srt.managers.io_struct import (
     UpdateWeightsFromDistributedReqOutput,
     UpdateWeightsFromIPCReqInput,
     UpdateWeightsFromIPCReqOutput,
+    UpdateWeightsFromChannelReqInput,
+    UpdateWeightsFromChannelReqOutput,
     UpdateWeightsFromTensorReqInput,
     UpdateWeightsFromTensorReqOutput,
     UpdateWeightVersionReqInput,
@@ -108,6 +110,7 @@ _COMMUNICATOR_SPECS = [
     ("send_weights_to_remote_instance", SendWeightsToRemoteInstanceReqOutput),
     ("update_weights_from_tensor", UpdateWeightsFromTensorReqOutput),
     ("update_weights_from_ipc", UpdateWeightsFromIPCReqOutput),
+    ("update_weights_from_channel", UpdateWeightsFromChannelReqOutput),
     ("update_weight_version", UpdateWeightVersionReqOutput),
     ("get_weights_by_name", GetWeightsByNameReqOutput),
     ("release_memory_occupation", ReleaseMemoryOccupationReqOutput),
@@ -579,6 +582,23 @@ class TokenizerControlMixin:
 
         return success, message
 
+    async def update_weights_from_channel(
+        self: TokenizerManager,
+        obj: UpdateWeightsFromChannelReqInput,
+        request: Optional[fastapi.Request] = None,
+    ) -> Tuple[bool, str]:
+        """Update weights by receiving from the TransferMesh channel."""
+        self.auto_create_handle_loop()
+        async with self.model_update_lock.writer_lock:
+            if obj.weight_version == "":
+                return False, "weight_version must not be empty"
+            results = await self.update_weights_from_channel_communicator(obj)
+            success, message = FanOutCommunicator.merge_results(results)
+            if success:
+                self._update_weight_version_if_provided(obj.weight_version)
+                message += f" Weight version updated to {obj.weight_version}."
+            return success, message
+
     async def _unload_lora_adapter_locked(
         self: TokenizerManager,
         obj: UnloadLoRAAdapterReqInput,
diff --git a/python/sglang/srt/managers/tp_worker.py b/python/sglang/srt/managers/tp_worker.py
index 275d376361..5f2751e664 100644
--- a/python/sglang/srt/managers/tp_worker.py
+++ b/python/sglang/srt/managers/tp_worker.py
@@ -232,6 +232,9 @@ class BaseTpWorker(ABC):
         )
         return success, message
 
+    def update_weights_from_channel(self, recv_req):
+        return self.model_runner.weight_updater.update_weights_from_channel(recv_req)
+
     def get_weights_by_name(self, recv_req: GetWeightsByNameReqInput):
         parameter = self.model_runner.weight_exporter.get_weights_by_name(
             recv_req.name, recv_req.truncate_size
diff --git a/python/sglang/srt/model_executor/model_runner_components/weight_updater.py b/python/sglang/srt/model_executor/model_runner_components/weight_updater.py
index 1668c966b1..8837ac11ee 100644
--- a/python/sglang/srt/model_executor/model_runner_components/weight_updater.py
+++ b/python/sglang/srt/model_executor/model_runner_components/weight_updater.py
@@ -406,6 +406,35 @@ class WeightUpdater:
             logger.error(f"IPC weight update failed: {e}")
             return False, str(e)
 
+    def update_weights_from_channel(self: WeightUpdater, recv_req):
+        """Receive weights from TransferMesh channel and update model in-place.
+
+        Uses coda.transfer_mesh.create_receiver_channel() to lazily create or reuse a
+        process-local TransferMeshChannel based on the request parameters.
+        """
+        try:
+            from coda.transfer_mesh import ChannelMeta, create_receiver_channel
+            from sglang.srt.utils.network import get_local_ip_auto
+
+            meta = ChannelMeta(
+                master_addr=recv_req.master_addr,
+                world_size=recv_req.world_size,
+                train_world_size=recv_req.train_world_size,
+                engine_id=recv_req.engine_id,
+                recreate=recv_req.recreate,
+                buffer_size_bytes=recv_req.buffer_size_bytes,
+                timeout=recv_req.timeout,
+                gloo_port=recv_req.gloo_port,
+                local_ip=get_local_ip_auto(),
+            )
+            channel = create_receiver_channel(meta)
+            self.get_model().load_weights(channel.get_iterator())
+            return True, "Succeeded to update weights from channel."
+        except Exception as e:
+            error_msg = f"Failed to update weights from channel: {e}."
+            logger.error(error_msg)
+            return False, error_msg
+
 
 def _model_load_weights_direct(model, named_tensors: List[Tuple[str, torch.Tensor]]):
     params_dict = dict(model.named_parameters())
diff --git a/python/sglang/srt/model_loader/loader.py b/python/sglang/srt/model_loader/loader.py
index 13284e46da..a55dfaf280 100644
--- a/python/sglang/srt/model_loader/loader.py
+++ b/python/sglang/srt/model_loader/loader.py
@@ -1635,6 +1635,13 @@ class DummyModelLoader(BaseModelLoader):
 
             _post_load_weights(model)
 
+            # dummy load fills every param with random values, but the engram fp8
+            # tables are frozen constants NOT carried by the RL weight update, so they
+            # must still be read from the checkpoint here (no-op for non-engram models).
+            from sglang.srt.layers.engram import load_engram_tables_from_disk
+
+            load_engram_tables_from_disk(model, model_config.model_path)
+
             for _, module in model.named_modules():
                 quant_method = getattr(module, "quant_method", None)
                 if quant_method is not None:
diff --git a/python/sglang/srt/model_loader/weight_utils.py b/python/sglang/srt/model_loader/weight_utils.py
index 98da6a4cbc..204fbf6c03 100644
--- a/python/sglang/srt/model_loader/weight_utils.py
+++ b/python/sglang/srt/model_loader/weight_utils.py
@@ -1719,6 +1719,13 @@ def initialize_dummy_weights(
     the parameter's number of elements and its data type.
     """
     for name, param in model.state_dict().items():
+        # The engram fp8 tables are huge (~98GB each). They are (re)loaded from the
+        # checkpoint by load_engram_tables_from_disk, so there is no need to fill them
+        # with random values here -- and doing so upcasts the full fp8 table to fp16
+        # (`param.data.to(torch.float16)`), allocating a ~2x (~196GB) PRIVATE temp per
+        # TP rank, which caused the colocate host OOM.
+        if ".engram.embed." in name:
+            continue
         if torch.is_floating_point(param):
             if name.endswith("weight_scale_inv"):
                 param.fill_(1.0)
